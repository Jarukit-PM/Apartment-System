# If `next dev` fails with "@parcel/watcher-linux-arm64-glibc", your `node_modules`
# likely came from another OS (e.g. macOS). Run: rm -rf node_modules && npm install
# in this directory on the machine where you develop.
