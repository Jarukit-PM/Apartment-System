{
  "name": "Apartment System (MongoDB + Go + Node)",
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspaces/Apartment-System",
  "features": {
    "ghcr.io/devcontainers/features/go:1": {
      "version": "1.25"
    },
    "ghcr.io/devcontainers/features/node:1": {
      "version": "22"
    }
  },
  "remoteEnv": {
    "MONGODB_URI": "mongodb://mongo:27017/apartment_system",
    "JWT_SECRET": "devcontainer-compose-local-not-for-production"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "golang.go"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "remoteUser": "vscode",
  "postCreateCommand": "bash -lc 'set -e; npm ci --prefix apps/web; go install github.com/air-verse/air@latest; echo Web deps installed. Air installed for Go live reload. JWT_SECRET and MONGODB_URI are set on the devcontainer service in docker-compose.yml — Rebuild Container after changing them.'",
  "postStartCommand": "bash -lc 'set -e; test -x apps/web/node_modules/.bin/next || npm ci --prefix apps/web'"
}
